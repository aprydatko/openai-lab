# Shared Helpers

Use this folder later for small reusable helpers, for example:

- `client.ts` for a shared OpenAI client
- `types.ts` for shared lesson types
- `console.ts` for pretty printing responses
- `mock-data.ts` for local incident examples
